name: tubesppbm
description: "A new Flutter project."

environment:
  sdk: "^3.6.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.0.15
  cupertino_icons: ^1.0.8
  video_player: ^2.3.8
  provider: ^6.1.2
  flutter_chat_ui: ^1.0.0  # Add this line (check for the latest version)
  firebase_core: ^2.14.0
  firebase_database: ^10.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/video.mp4  # Correct indentation
